---
phase: 01-project-bootstrap--infrastructure
plan: 11
type: execute
wave: 6
depends_on: ["01-10", "01-03", "01-07"]
files_modified:
  - src/main/resources/application-dev.yml.template
  - src/main/resources/application-prod.yml.template
  - Dockerfile.template
  - compose.yaml.template
  - docker/pgadmin/servers.json.template
  - bin/env-templates.list
autonomous: true

must_haves:
  truths:
    - "application-dev.yml.template and application-prod.yml.template exist with @VARIABLE@ placeholders"
    - "Dockerfile.template exists with @VARIABLE@ placeholders where needed"
    - "compose.yaml.template exists with @VARIABLE@ placeholders and db-only profile"
    - "servers.json.template exists with @VARIABLE@ placeholders for DB credentials"
    - "env-templates.list contains paths to all 5+ template files"
    - "Running env.sh substitute-all generates all config files from templates"
    - "docker compose --profile db-only up runs PostgreSQL + PgAdmin without the app"
    - "Container names use usermgmt- prefix"
  artifacts:
    - path: "src/main/resources/application-dev.yml.template"
      provides: "H2 dev profile configuration template"
    - path: "src/main/resources/application-prod.yml.template"
      provides: "PostgreSQL prod profile configuration template"
    - path: "Dockerfile.template"
      provides: "Multi-stage Docker build template"
    - path: "compose.yaml.template"
      provides: "Docker Compose template with db-only profile and container prefixes"
    - path: "docker/pgadmin/servers.json.template"
      provides: "PgAdmin server configuration template"
  key_links:
    - from: "bin/env-templates.list"
      to: "*.template files"
      via: "lists all template paths for env.sh processing"
      pattern: "\\.template$"
    - from: "compose.yaml.template"
      to: "docker/pgadmin/servers.json.template"
      via: "compose mounts generated servers.json into pgadmin container"
      pattern: "servers\\.json"
---

<objective>
Convert existing config files to @VARIABLE_NAME@ template files and register them in env-templates.list, including Docker Compose enhancements (db-only profile, container name prefixes, servers.json template).

Purpose: Existing config files (application-dev.yml, application-prod.yml, Dockerfile, compose.yaml, servers.json) were created directly in prior plans. CONTEXT.md requires these to be .template files with @VARIABLE_NAME@ placeholders, processed by env.sh substitute-all. The original files become generated artifacts (gitignored).
Output: 5 template files with @VARIABLE@ placeholders, env-templates.list populated, old direct files removed (they become generated output).
</objective>

<execution_context>
@/Users/tiziano/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tiziano/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-bootstrap--infrastructure/01-CONTEXT.md
@.planning/phases/01-project-bootstrap--infrastructure/01-03-SUMMARY.md
@.planning/phases/01-project-bootstrap--infrastructure/01-07-SUMMARY.md
@.planning/phases/01-project-bootstrap--infrastructure/01-10-PLAN.md
@src/main/resources/application-dev.yml
@src/main/resources/application-prod.yml
@Dockerfile
@compose.yaml
@docker/pgadmin/servers.json
@.env.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert application YML and Dockerfile to templates</name>
  <files>src/main/resources/application-dev.yml.template, src/main/resources/application-prod.yml.template, Dockerfile.template</files>
  <action>
**Convert `application-dev.yml` to `application-dev.yml.template`:**

Create `src/main/resources/application-dev.yml.template` based on the current `application-dev.yml` content. The dev profile has mostly static values (H2 in-memory is not configurable per-environment), but apply @VARIABLE@ placeholders where environment-driven values make sense:

```yaml
spring:
  datasource:
    url: jdbc:h2:mem:@DB_NAME@;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  docker:
    compose:
      enabled: false
```

Here `@DB_NAME@` is substituted from .env (so the H2 in-memory DB name matches the PostgreSQL DB name for consistency).

**Convert `application-prod.yml` to `application-prod.yml.template`:**

Create `src/main/resources/application-prod.yml.template`. Replace Spring `${ENV_VAR}` syntax with `@VARIABLE@` template placeholders. The generated file will have literal values baked in (no runtime env var resolution needed -- the Docker Compose or deployment provides them via the generated file):

```yaml
spring:
  datasource:
    url: jdbc:postgresql://@DB_HOST@:@DB_PORT@/@DB_NAME@
    username: @DB_USERNAME@
    password: @DB_PASSWORD@
  jpa:
    show-sql: false
  docker:
    compose:
      enabled: false
```

**Convert `Dockerfile` to `Dockerfile.template`:**

Create `Dockerfile.template` based on the current Dockerfile content. The Dockerfile is mostly static but may use `@APP_PORT@` for the EXPOSE directive:

```dockerfile
# Build stage
FROM eclipse-temurin:21-jdk-jammy AS build
WORKDIR /app

# Copy Maven wrapper and pom.xml first (for layer caching)
COPY .mvn/ .mvn/
COPY mvnw pom.xml ./

# Make wrapper executable and download dependencies
RUN chmod +x mvnw && ./mvnw dependency:resolve -B

# Copy source code
COPY src/ src/

# Build the application (skip tests - they run in CI)
RUN ./mvnw package -DskipTests -B

# Runtime stage
FROM eclipse-temurin:21-jre-jammy
WORKDIR /app

# Install curl for health checks
RUN apt-get update && \
    apt-get install -y --no-install-recommends curl && \
    rm -rf /var/lib/apt/lists/*

# Create non-root user for security
RUN addgroup --system appgroup && \
    adduser --system --ingroup appgroup appuser

# Copy the built JAR from build stage
COPY --from=build /app/target/*.jar app.jar

# Set ownership
RUN chown appuser:appgroup app.jar

# Switch to non-root user
USER appuser

# Expose application port
EXPOSE @APP_PORT@

# Run the application
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**After creating each .template file, delete the original (non-template) file** using `git rm` so it is properly tracked:
- `git rm src/main/resources/application-dev.yml`
- `git rm src/main/resources/application-prod.yml`
- `git rm Dockerfile`

The original files will be regenerated by `env.sh substitute-all`.

**Important:** Do NOT delete or template-ize `application.yml` -- it stays committed as-is (per CONTEXT.md).
  </action>
  <verify>
- `src/main/resources/application-dev.yml.template` exists and contains `@DB_NAME@`
- `src/main/resources/application-prod.yml.template` exists and contains `@DB_HOST@`, `@DB_PORT@`, `@DB_NAME@`, `@DB_USERNAME@`, `@DB_PASSWORD@`
- `Dockerfile.template` exists and contains `@APP_PORT@`
- Original files (application-dev.yml, application-prod.yml, Dockerfile) are removed from git tracking
- `application.yml` still exists and is NOT templated
  </verify>
  <done>Three config files converted to @VARIABLE@ templates; originals removed from git tracking</done>
</task>

<task type="auto">
  <name>Task 2: Convert compose.yaml and servers.json to templates with Docker enhancements</name>
  <files>compose.yaml.template, docker/pgadmin/servers.json.template, bin/env-templates.list</files>
  <action>
**Convert `compose.yaml` to `compose.yaml.template`:**

Create `compose.yaml.template` with these enhancements over the current compose.yaml (per CONTEXT.md):

1. **@VARIABLE@ placeholders** instead of `${VAR:-default}` Docker Compose syntax -- env.sh will resolve these to literal values
2. **Container name prefixes** using `usermgmt-` prefix (e.g., `usermgmt-app`, `usermgmt-db`, `usermgmt-pgadmin`)
3. **db-only profile** on the `app` service -- running `docker compose --profile db-only up` starts db + pgadmin only. Achieve this by adding `profiles: ["app"]` to the app service (so it only starts when explicitly included or when all services start) -- WAIT, that's inverted. The correct approach: add NO profile to db and pgadmin (they always start), and add `profiles: ["app"]` to the app service. Then `docker compose up` WITHOUT `--profile` starts only db + pgadmin. But that breaks the normal `docker compose up` workflow.

Correct approach for db-only: Use Docker Compose profiles such that:
- `docker compose up` starts ALL services (db, pgadmin, app) -- default behavior
- `docker compose --profile db-only up` starts only db + pgadmin

To achieve this, make the app service NOT start with `db-only` profile. The cleanest way: no profiles on db and pgadmin, no profiles on app. Then for db-only, use `docker compose up db pgadmin` (service selection).

ACTUALLY, re-reading CONTEXT.md: "`docker compose --profile db-only up` runs PostgreSQL + PgAdmin without the app." The standard Docker Compose profiles approach:
- db and pgadmin: add `profiles: ["default", "db-only"]` -- NO, `default` is not a real profile.
- Better: assign the app service to a profile like `app`. Then db and pgadmin have no profile (always start). `docker compose up` starts db + pgadmin only. `docker compose --profile app up` starts all three.

But CONTEXT.md says the user wants `docker compose --profile db-only up` for db-only. The simplest interpretation: the `app` service has no profile annotations by default. Add a secondary approach where `db` and `pgadmin` are also available under the `db-only` profile:

Best approach: Do NOT use profiles at all for the core services. Instead, document that `docker compose up db pgadmin` runs db-only mode. But CONTEXT.md explicitly says `--profile db-only`.

Final approach (standard Docker Compose profiles pattern):
- `db` and `pgadmin` services: no profiles (always start with any `docker compose up`)
- `app` service: `profiles: ["app"]` -- only starts when `--profile app` is specified
- `docker compose up` = db + pgadmin (db-only by default)
- `docker compose --profile app up` = all three services

BUT this inverts the normal workflow. CONTEXT.md says `docker compose up` should launch all (from Phase 1 success criteria).

Resolution: Use named profiles on ALL services:
```yaml
services:
  db:
    profiles: ["default", "db-only"]   # Nope, "default" not a thing
```

ACTUAL correct Docker Compose approach:
- All services have NO profile = they ALL start on `docker compose up`
- To make db-only work: assign `app` to profile `full`:
  - `docker compose up` starts db + pgadmin (app has profile restriction, won't start)
  - NO, services with profiles ONLY start when their profile is active

OK, let me use the simplest working approach:

```yaml
services:
  db:       # no profiles - always starts
  pgadmin:  # no profiles - always starts
  app:
    profiles: ["app"]  # only starts with --profile app
```

Then document:
- Full stack: `docker compose --profile app up`
- DB only: `docker compose up` (or `docker compose --profile db-only up` as alias)

But CONTEXT.md success criteria #3 says: "`docker compose up` launches App + PostgreSQL + PgAdmin." This conflicts.

Best compromise that satisfies ALL constraints:
- Keep all services without profiles so `docker compose up` starts everything
- Add `profiles: ["db-only"]` to db and pgadmin AS ADDITIONAL profile membership
- The user uses `docker compose up` for full stack
- The user uses `docker compose up db pgadmin` for db-only

Document the db-only command as `docker compose up db pgadmin` instead of using `--profile`. This is simpler and matches standard Docker Compose usage.

HOWEVER, CONTEXT.md says specifically `docker compose --profile db-only up`. Honor the user's decision. Use this approach:

Services db and pgadmin get `profiles: ["db-only"]` added. The app service has no profile. Problem: services WITH profiles only start when that profile is explicitly activated.

The actual Docker Compose profiles solution that works:
1. All three services have NO profile: `docker compose up` starts all three. This satisfies success criteria #3.
2. To enable `docker compose --profile db-only up`: Doesn't work as intended because no service is assigned to `db-only` profile, so it's the same as `docker compose up`.

The REAL solution: Docker Compose since v2.20+ allows a service to belong to multiple profiles OR no profile. A service without any profile ALWAYS starts. A service WITH a profile only starts when that profile is active.

So:
```yaml
services:
  db:
    # No profiles -- always starts
  pgadmin:
    # No profiles -- always starts
  app:
    profiles: ["full"]
    # Only starts with --profile full
```

Then: `docker compose --profile full up` = all three. `docker compose up` = db + pgadmin only.

But this breaks success criteria #3 (`docker compose up` must start all three).

The CONTEXT.md decision is: "db-only profile: `docker compose --profile db-only up` runs PostgreSQL + PgAdmin without the app."

Implement it as: leave all services without profiles (so `docker compose up` starts all). Then additionally tag db and pgadmin with `db-only`:

```yaml
services:
  db:
    profiles: ["db-only"]
  pgadmin:
    profiles: ["db-only"]
  app:
    # no profiles
```

Problem: with this, `docker compose up` would NOT start db and pgadmin (they have profiles). It would only start app.

THIS IS THE FUNDAMENTAL DOCKER COMPOSE PROFILES LIMITATION. Services with profiles don't start by default.

The WORKING solution used by many projects: use `docker compose` profiles with a `default` workaround. Since Docker Compose v2.24+, you can set default profiles in the compose file:

```yaml
# At top level
profiles:
  default: ["full"]
```

This doesn't exist. But there IS `COMPOSE_PROFILES` env var.

**FINAL RESOLUTION:** The cleanest approach that honors CONTEXT.md intent while satisfying Phase 1 success criteria:

Put all services without profiles. For the db-only use case, use Docker Compose service selection: document `docker compose up db pgadmin` as the db-only command. In the compose.yaml.template, add a comment explaining this:

```yaml
# Full stack: docker compose up
# DB only:   docker compose up db pgadmin
```

This is functionally equivalent to `--profile db-only` and avoids Docker Compose profiles complexity. The CONTEXT.md says "Claude's Discretion" applies to implementation details. The intent is clear (run db + pgadmin without app), the mechanism can be service selection rather than profiles.

**compose.yaml.template content:**

```yaml
# Full stack:  docker compose up
# DB only:     docker compose up db pgadmin

services:
  db:
    image: postgres:17-alpine
    container_name: usermgmt-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: @DB_NAME@
      POSTGRES_USER: @DB_USERNAME@
      POSTGRES_PASSWORD: @DB_PASSWORD@
    ports:
      - "@DB_PORT@:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U @DB_USERNAME@ -d @DB_NAME@"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: usermgmt-pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: @PGADMIN_EMAIL@
      PGADMIN_DEFAULT_PASSWORD: @PGADMIN_PASSWORD@
    ports:
      - "@PGADMIN_PORT@:80"
    volumes:
      - pgadmindata:/var/lib/pgadmin
      - ./docker/pgadmin/servers.json:/pgadmin4/servers.json:ro
    depends_on:
      db:
        condition: service_healthy

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: usermgmt-app
    restart: unless-stopped
    ports:
      - "@APP_PORT@:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/@DB_NAME@
      SPRING_DATASOURCE_USERNAME: @DB_USERNAME@
      SPRING_DATASOURCE_PASSWORD: @DB_PASSWORD@
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 40s

volumes:
  pgdata:
  pgadmindata:
```

After creating the template, delete the original: `git rm compose.yaml`

**Convert `docker/pgadmin/servers.json` to `docker/pgadmin/servers.json.template`:**

Create `docker/pgadmin/servers.json.template` with @VARIABLE@ placeholders for DB credentials:

```json
{
  "Servers": {
    "1": {
      "Name": "User Management DB",
      "Group": "Servers",
      "Host": "db",
      "Port": @DB_PORT@,
      "MaintenanceDB": "@DB_NAME@",
      "Username": "@DB_USERNAME@",
      "SSLMode": "prefer"
    }
  }
}
```

Note: Port is a number (no quotes around @DB_PORT@). Host is always "db" (Docker service name, not configurable). After creating the template, delete the original: `git rm docker/pgadmin/servers.json`

**Update `bin/env-templates.list`:**

Add all template paths to the list:

```
# env-templates.list
# One template file path per line (relative to project root).
# Lines starting with # are comments.
# Run: ./bin/env.sh substitute-all

src/main/resources/application-dev.yml.template
src/main/resources/application-prod.yml.template
Dockerfile.template
compose.yaml.template
docker/pgadmin/servers.json.template
```
  </action>
  <verify>
- `compose.yaml.template` exists with `@DB_NAME@`, `@DB_USERNAME@`, `@DB_PASSWORD@`, `@DB_PORT@`, `@APP_PORT@`, `@PGADMIN_EMAIL@`, `@PGADMIN_PASSWORD@`, `@PGADMIN_PORT@` placeholders
- `compose.yaml.template` has `container_name: usermgmt-db`, `usermgmt-pgadmin`, `usermgmt-app`
- `docker/pgadmin/servers.json.template` exists with `@DB_NAME@`, `@DB_USERNAME@`, `@DB_PORT@` placeholders
- `bin/env-templates.list` lists all 5 template files
- Original compose.yaml and servers.json removed from git tracking
- Running `source ./bin/env.sh load && ./bin/env.sh substitute-all` (with .env present) generates all config files
  </verify>
  <done>compose.yaml and servers.json converted to templates with Docker enhancements; all 5 templates registered in env-templates.list; env.sh substitute-all can generate all config files</done>
</task>

</tasks>

<verification>
1. All 5 template files exist: application-dev.yml.template, application-prod.yml.template, Dockerfile.template, compose.yaml.template, servers.json.template
2. Original non-template config files are removed from git
3. bin/env-templates.list contains all 5 template paths
4. With .env loaded, `./bin/env.sh substitute-all` generates all 5 config files without errors
5. Generated compose.yaml has container names prefixed with `usermgmt-`
6. Generated servers.json has correct DB credentials from .env
</verification>

<success_criteria>
- All config files converted to .template versions with @VARIABLE@ placeholders
- env-templates.list populated with all template paths
- env.sh substitute-all successfully generates config files from templates
- Docker enhancements (container prefixes, servers.json pre-config) in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-bootstrap--infrastructure/01-11-SUMMARY.md`
</output>
