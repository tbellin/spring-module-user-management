---
phase: 01-project-bootstrap--infrastructure
plan: 06
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - src/main/java/com/example/usermanagement/shared/web/HomeController.java
  - src/main/resources/templates/index.html
  - src/main/resources/templates/layout/default.html
autonomous: true

must_haves:
  truths:
    - "Visiting / returns 200 OK"
    - "Home page displays application name and active profile"
    - "Bootstrap 5 styling is applied via WebJars"
  artifacts:
    - path: "src/main/java/com/example/usermanagement/shared/web/HomeController.java"
      provides: "Home page controller"
      contains: "@GetMapping"
    - path: "src/main/resources/templates/index.html"
      provides: "Home page Thymeleaf template"
      contains: "th:text"
    - path: "src/main/resources/templates/layout/default.html"
      provides: "Thymeleaf layout template"
      contains: "layout:fragment"
  key_links:
    - from: "HomeController.java"
      to: "index.html"
      via: "return view name"
      pattern: "return.*index"
    - from: "index.html"
      to: "default.html"
      via: "layout:decorate"
      pattern: "layout:decorate"
---

<objective>
Create the home page controller and Thymeleaf templates with Bootstrap 5 styling.

Purpose: Provides a working smoke test page to verify the application starts correctly. Meets PAGE-01 (home page accessible to all visitors).
Output: HomeController.java, index.html, and layout template
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-bootstrap--infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Thymeleaf layout template</name>
  <files>src/main/resources/templates/layout/default.html</files>
  <action>
Create the base layout template with Bootstrap 5:

**src/main/resources/templates/layout/default.html:**
```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title layout:title-pattern="$CONTENT_TITLE - $LAYOUT_TITLE">User Management</title>
    <link rel="stylesheet" th:href="@{/webjars/bootstrap/5.3.3/css/bootstrap.min.css}">
    <link rel="stylesheet" th:href="@{/css/style.css}" th:if="${#ctx.containsKey('cssFile')}">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" th:href="@{/}">User Management</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" th:href="@{/}">Home</a>
                    </li>
                    <!-- Additional nav items will be added in later phases -->
                </ul>
            </div>
        </div>
    </nav>

    <main class="container mt-4">
        <div layout:fragment="content">
            <!-- Page content will be inserted here -->
        </div>
    </main>

    <footer class="footer mt-auto py-3 bg-light">
        <div class="container text-center">
            <span class="text-muted">User Management System</span>
        </div>
    </footer>

    <script th:src="@{/webjars/bootstrap/5.3.3/js/bootstrap.bundle.min.js}"></script>
</body>
</html>
```

Create directory: src/main/resources/templates/layout/

Layout features:
- Bootstrap 5 CSS and JS via WebJars
- Responsive navbar with collapse for mobile
- Title pattern that combines page title with site name
- layout:fragment="content" placeholder for page content
  </action>
  <verify>
Run: grep -E "layout:fragment|bootstrap" src/main/resources/templates/layout/default.html | wc -l
Expected: At least 3 (fragment, CSS, JS references)
  </verify>
  <done>Layout template exists with Bootstrap 5 and Thymeleaf layout support</done>
</task>

<task type="auto">
  <name>Task 2: Create home page template</name>
  <files>src/main/resources/templates/index.html</files>
  <action>
Create the home page template:

**src/main/resources/templates/index.html:**
```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="~{layout/default}">
<head>
    <title>Home</title>
</head>
<body>
    <div layout:fragment="content">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <h1 class="mb-4">User Management System</h1>
                <p class="lead">Application is running successfully.</p>

                <div class="card mt-4">
                    <div class="card-body">
                        <h5 class="card-title">System Status</h5>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item d-flex justify-content-between">
                                <span>Active Profile</span>
                                <span class="badge bg-primary" th:text="${activeProfile}">unknown</span>
                            </li>
                            <li class="list-group-item d-flex justify-content-between">
                                <span>Application Status</span>
                                <span class="badge bg-success">Running</span>
                            </li>
                        </ul>
                    </div>
                </div>

                <div class="alert alert-info mt-4" th:if="${activeProfile == 'dev'}">
                    <strong>Development Mode:</strong> H2 Console available at
                    <a th:href="@{/h2-console}">/h2-console</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
```

Template features:
- Uses layout:decorate to extend default layout
- Displays active Spring profile
- Shows H2 console link only in dev mode
- Bootstrap card component for system status
  </action>
  <verify>
Run: grep -E "layout:decorate|activeProfile" src/main/resources/templates/index.html
Expected: Both layout:decorate and activeProfile references found
  </verify>
  <done>Home page template exists with profile display and H2 console link</done>
</task>

<task type="auto">
  <name>Task 3: Create HomeController</name>
  <files>src/main/java/com/example/usermanagement/shared/web/HomeController.java</files>
  <action>
Create the home controller:

**src/main/java/com/example/usermanagement/shared/web/HomeController.java:**
```java
package com.example.usermanagement.shared.web;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.core.env.Environment;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;

/**
 * Controller for the home page.
 * Part of the shared module - accessible to all visitors.
 */
@Controller
public class HomeController {

    private final Environment environment;

    @Autowired
    public HomeController(Environment environment) {
        this.environment = environment;
    }

    @GetMapping("/")
    public String home(Model model) {
        String[] activeProfiles = environment.getActiveProfiles();
        String activeProfile = activeProfiles.length > 0 ? activeProfiles[0] : "default";
        model.addAttribute("activeProfile", activeProfile);
        return "index";
    }
}
```

Create directory: src/main/java/com/example/usermanagement/shared/web/

Controller responsibilities:
- Handle GET / request
- Inject active Spring profile into model
- Return "index" view name
  </action>
  <verify>
Run: grep -E "@GetMapping|@Controller" src/main/java/com/example/usermanagement/shared/web/HomeController.java
Expected: Both annotations found
  </verify>
  <done>HomeController exists with root mapping and profile injection</done>
</task>

</tasks>

<verification>
1. src/main/resources/templates/layout/default.html exists with Bootstrap 5 CDN
2. src/main/resources/templates/index.html exists and uses layout:decorate
3. index.html displays activeProfile from model
4. index.html shows H2 console link only when activeProfile is "dev"
5. HomeController.java exists in shared.web package
6. HomeController maps GET / to "index" view
7. HomeController injects active profile into model
</verification>

<success_criteria>
- Layout template provides consistent Bootstrap 5 styling
- Home page extends layout and displays system status
- H2 console link visible only in dev mode
- Controller correctly passes active profile to view
- Page accessible without authentication (verified by SecurityConfig from Plan 04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-bootstrap--infrastructure/01-06-SUMMARY.md`
</output>
