---
phase: 01-project-bootstrap--infrastructure
plan: 08
type: execute
wave: 4
depends_on: ["01-04", "01-05", "01-06"]
files_modified:
  - src/test/java/com/example/usermanagement/ApplicationTests.java
  - src/test/java/com/example/usermanagement/ModularityTests.java
autonomous: true

must_haves:
  truths:
    - "./mvnw test passes all tests"
    - "Application context loads successfully"
    - "Spring Modulith verifies module boundaries"
  artifacts:
    - path: "src/test/java/com/example/usermanagement/ApplicationTests.java"
      provides: "Smoke test verifying application context loads"
      contains: "@SpringBootTest"
    - path: "src/test/java/com/example/usermanagement/ModularityTests.java"
      provides: "Module boundary verification test"
      contains: "ApplicationModules"
  key_links:
    - from: "ApplicationTests.java"
      to: "Application.java"
      via: "Spring context"
      pattern: "@SpringBootTest"
    - from: "ModularityTests.java"
      to: "package-info.java files"
      via: "ApplicationModules.of"
      pattern: "ApplicationModules\\.of"
---

<objective>
Create test files to verify application startup and Spring Modulith module boundaries.

Purpose: Ensures the bootstrap phase produces a working, well-structured application. Tests validate that all configuration is correct and module boundaries are enforced.
Output: ApplicationTests.java (smoke test) and ModularityTests.java (module verification)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-bootstrap--infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ApplicationTests.java</name>
  <files>src/test/java/com/example/usermanagement/ApplicationTests.java</files>
  <action>
Create the smoke test that verifies application context loads:

**src/test/java/com/example/usermanagement/ApplicationTests.java:**
```java
package com.example.usermanagement;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

/**
 * Smoke test to verify the application context loads successfully.
 * Uses the dev profile to test with H2 in-memory database.
 */
@SpringBootTest
@ActiveProfiles("dev")
class ApplicationTests {

    @Test
    void contextLoads() {
        // If this test passes, the application context loaded successfully.
        // This verifies:
        // - All beans are properly configured
        // - Database connection works (H2 in dev profile)
        // - Flyway migrations run successfully
        // - Security configuration is valid
    }

}
```

Create directory: src/test/java/com/example/usermanagement/

Key points:
- @SpringBootTest loads full application context
- @ActiveProfiles("dev") uses H2 database (no external dependencies)
- Empty test body is intentional - context loading IS the test
  </action>
  <verify>
Run: grep -E "@SpringBootTest|@ActiveProfiles" src/test/java/com/example/usermanagement/ApplicationTests.java
Expected: Both annotations found
  </verify>
  <done>ApplicationTests.java exists with context load test</done>
</task>

<task type="auto">
  <name>Task 2: Create ModularityTests.java</name>
  <files>src/test/java/com/example/usermanagement/ModularityTests.java</files>
  <action>
Create the Spring Modulith verification test:

**src/test/java/com/example/usermanagement/ModularityTests.java:**
```java
package com.example.usermanagement;

import org.junit.jupiter.api.Test;
import org.springframework.modulith.core.ApplicationModules;

/**
 * Tests that verify the modular structure of the application.
 * These tests ensure module boundaries are respected and
 * dependencies between modules are properly declared.
 */
class ModularityTests {

    ApplicationModules modules = ApplicationModules.of(Application.class);

    @Test
    void verifiesModularStructure() {
        // This test fails if:
        // - A module accesses another module's internal types
        // - A module depends on another module not in its allowedDependencies
        // - Circular dependencies exist between modules
        modules.verify();
    }

    @Test
    void printsModuleArrangement() {
        // Outputs the detected module structure to console.
        // Useful for debugging and documentation.
        modules.forEach(System.out::println);
    }

}
```

Key points (from RESEARCH.md):
- ApplicationModules.of() scans from the main Application class
- verify() checks module boundaries match allowedDependencies in package-info.java
- printsModuleArrangement() is informational - helps debug module detection
- This is a unit test (not @SpringBootTest) - fast execution
  </action>
  <verify>
Run: grep -E "ApplicationModules|modules.verify" src/test/java/com/example/usermanagement/ModularityTests.java
Expected: Both ApplicationModules and verify() found
  </verify>
  <done>ModularityTests.java exists with module boundary verification</done>
</task>

</tasks>

<verification>
1. src/test/java/com/example/usermanagement/ApplicationTests.java exists
2. ApplicationTests has @SpringBootTest and @ActiveProfiles("dev")
3. src/test/java/com/example/usermanagement/ModularityTests.java exists
4. ModularityTests uses ApplicationModules.of(Application.class)
5. ModularityTests calls modules.verify()
6. ./mvnw test passes (all tests green)
</verification>

<success_criteria>
- ApplicationTests verifies Spring context loads with dev profile
- ModularityTests verifies module boundaries are respected
- ./mvnw test -Dspring.profiles.active=dev passes without failures
- Module verification catches any boundary violations
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-bootstrap--infrastructure/01-08-SUMMARY.md`
</output>
