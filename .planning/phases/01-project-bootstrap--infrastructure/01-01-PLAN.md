---
phase: 01-project-bootstrap--infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - mvnw
  - mvnw.cmd
  - .mvn/wrapper/maven-wrapper.properties
  - src/main/java/com/example/usermanagement/Application.java
autonomous: true

must_haves:
  truths:
    - "Maven build compiles without errors"
    - "Application class exists with @SpringBootApplication annotation"
    - "Maven wrapper is executable and works"
  artifacts:
    - path: "pom.xml"
      provides: "Spring Boot 4.0.1 parent POM with all required dependencies"
      contains: "spring-boot-starter-parent"
    - path: "mvnw"
      provides: "Maven wrapper script"
    - path: "src/main/java/com/example/usermanagement/Application.java"
      provides: "Spring Boot application entry point"
      contains: "@SpringBootApplication"
  key_links:
    - from: "pom.xml"
      to: "Application.java"
      via: "Maven compile"
      pattern: "spring-boot-maven-plugin"
---

<objective>
Create the Maven project skeleton with Spring Boot 4.0.1 parent POM, all required dependencies, and the application entry point.

Purpose: This is the foundation for all subsequent plans. Nothing else can be built without a valid Maven project structure.
Output: Compilable Maven project with Spring Boot 4 dependencies and Application.java entry point.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-bootstrap--infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pom.xml with Spring Boot 4.0.1 dependencies</name>
  <files>pom.xml</files>
  <action>
Create pom.xml with the following structure:

1. Parent: spring-boot-starter-parent 4.0.1
2. Properties: java.version=21
3. DependencyManagement: spring-modulith-bom 2.0.1
4. Dependencies (all from RESEARCH.md Maven POM section):
   - spring-boot-starter-webmvc (RENAMED from web in Boot 4)
   - spring-boot-starter-security
   - spring-boot-starter-data-jpa
   - spring-boot-starter-thymeleaf
   - thymeleaf-extras-springsecurity6
   - thymeleaf-layout-dialect
   - spring-boot-starter-validation
   - spring-boot-starter-mail
   - spring-boot-starter-actuator
   - spring-modulith-starter-core
   - spring-boot-starter-flyway (NEW in Boot 4 - required)
   - flyway-database-postgresql
   - h2 (runtime scope)
   - spring-boot-h2console (NEW in Boot 4 - no hyphen before console)
   - postgresql (runtime scope)
   - bootstrap 5.3.3 (WebJars)
   - webjars-locator-core
   - spring-boot-devtools (runtime, optional)
   - spring-boot-docker-compose (runtime, optional)
   - spring-boot-starter-test (test scope)
   - spring-security-test (test scope)
   - spring-modulith-starter-test (test scope)
5. Build plugin: spring-boot-maven-plugin

GroupId: com.example
ArtifactId: user-management
Version: 0.0.1-SNAPSHOT
Name: User Management Server
Description: Spring Boot 4 user management application

CRITICAL: Use spring-boot-starter-webmvc NOT spring-boot-starter-web (renamed in Boot 4).
CRITICAL: Use spring-boot-h2console (no hyphen before console) for H2 web console.
CRITICAL: Use spring-boot-starter-flyway (starter required in Boot 4, not bare flyway-core).
  </action>
  <verify>
Run: ./mvnw validate -B
Expected: BUILD SUCCESS
  </verify>
  <done>pom.xml exists with all dependencies, Maven validate passes</done>
</task>

<task type="auto">
  <name>Task 2: Create Maven wrapper</name>
  <files>mvnw, mvnw.cmd, .mvn/wrapper/maven-wrapper.properties</files>
  <action>
Generate Maven wrapper using:
mvn wrapper:wrapper -Dmaven=3.9.9

This creates:
- mvnw (Unix script)
- mvnw.cmd (Windows script)
- .mvn/wrapper/maven-wrapper.properties
- .mvn/wrapper/maven-wrapper.jar (downloaded on first use)

After generation, ensure mvnw is executable: chmod +x mvnw
  </action>
  <verify>
Run: ./mvnw --version
Expected: Apache Maven 3.9.x output
  </verify>
  <done>Maven wrapper files exist and ./mvnw --version works</done>
</task>

<task type="auto">
  <name>Task 3: Create Application.java entry point</name>
  <files>src/main/java/com/example/usermanagement/Application.java</files>
  <action>
Create the Spring Boot application entry point:

```java
package com.example.usermanagement;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

Create directory structure: src/main/java/com/example/usermanagement/

The class is minimal - just @SpringBootApplication and main method.
  </action>
  <verify>
Run: ./mvnw compile -B
Expected: BUILD SUCCESS (compilation completes without errors)
  </verify>
  <done>Application.java compiles successfully</done>
</task>

</tasks>

<verification>
1. pom.xml exists with Spring Boot 4.0.1 parent
2. pom.xml contains spring-boot-starter-webmvc (not web)
3. pom.xml contains spring-boot-starter-flyway
4. pom.xml contains spring-boot-h2console
5. ./mvnw --version returns Maven 3.9.x
6. ./mvnw compile succeeds
</verification>

<success_criteria>
- Maven project structure is valid
- All Spring Boot 4 dependencies are declared correctly
- Application.java has @SpringBootApplication annotation
- ./mvnw compile passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-bootstrap--infrastructure/01-01-SUMMARY.md`
</output>
