---
phase: 01-project-bootstrap--infrastructure
plan: 09
type: execute
wave: 7
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05", "01-06", "01-07", "01-08", "01-10", "01-11", "01-12"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Application starts in dev mode with H2"
    - "Application starts in prod mode with PostgreSQL via Docker"
    - "env.sh substitute-all generates all config files from templates"
    - "Flyway uses vendor-specific migration directories"
    - "Dev mode seeds test user tiziano with ADMIN role"
    - "Startup banner shows mode-specific information"
    - "All Phase 1 success criteria are met"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 1 implementation meets all success criteria through manual testing and automated verification.

Purpose: Final verification before marking Phase 1 complete. Ensures all requirements (INFRA-01 through INFRA-06, TOOL-01, TOOL-04) are satisfied.
Output: Verification confirmation and any issues identified
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-bootstrap--infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification</name>
  <files></files>
  <action>
Execute automated verification steps:

1. Verify env.sh CLI works:
   ```bash
   ./bin/env.sh help
   ./bin/env.sh check
   ```
   Expected: help shows all subcommands; check reports template status

2. Generate config files from templates:
   ```bash
   source ./bin/env.sh load
   ./bin/env.sh substitute-all
   ```
   Expected: All template files processed, config files generated

3. Run all tests:
   ```bash
   ./mvnw test
   ```
   Expected: All tests pass (ApplicationTests, ModularityTests, SchemaComparisonTests)

4. Start application in dev mode and verify:
   ```bash
   ./mvnw spring-boot:run -Dspring-boot.run.profiles=dev &
   sleep 15
   curl -s http://localhost:8080/ | grep -q "User Management" && echo "HOME: OK" || echo "HOME: FAILED"
   curl -s http://localhost:8080/actuator/health | grep -q "UP" && echo "HEALTH: OK" || echo "HEALTH: FAILED"
   curl -s http://localhost:8080/h2-console | grep -q "H2 Console" && echo "H2 CONSOLE: OK" || echo "H2 CONSOLE: FAILED"
   # Stop the application
   pkill -f "spring-boot:run" || true
   ```
   Expected: Startup logs show [DEV MODE] banner and Flyway running from h2/ directory

5. Build Docker image:
   ```bash
   docker build -t user-management:test .
   ```
   Expected: Build succeeds

Report results of each step.
  </action>
  <verify>
All 5 steps complete successfully:
- env.sh CLI runs and reports template status
- Config files generated from templates
- Tests pass (including SchemaComparisonTests)
- Dev mode serves home page, actuator health, and H2 console with startup banner
- Docker build succeeds
  </verify>
  <done>Automated verification passes all checks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 1 infrastructure:
- Spring Boot 4.0.1 project with all dependencies
- Dual-database configuration (H2 dev, PostgreSQL prod)
- Flyway vendor-specific migrations (h2/ and postgresql/ directories)
- Docker Compose with App + PostgreSQL + PgAdmin (usermgmt- container prefixes)
- env.sh CLI tool with template processing (substitute-all, check, clean, load, show, help)
- .template files with @VARIABLE@ placeholders for all config files
- Home page with Bootstrap 5 styling
- Spring Modulith module structure
- Startup banner showing mode and database type
- Dev seed data (user tiziano with ADMIN role)
- Schema comparison test verifying dev/prod migration parity
  </what-built>
  <how-to-verify>
**Template System Verification:**
1. Run: `./bin/env.sh clean` to remove any generated files
2. Run: `source ./bin/env.sh load` to load environment
3. Run: `./bin/env.sh substitute-all` to generate config files
4. Verify generated files exist: compose.yaml, Dockerfile, application-dev.yml, application-prod.yml, servers.json

**Dev Mode Verification:**
1. Run: `./mvnw spring-boot:run -Dspring-boot.run.profiles=dev`
2. Check startup logs for: `[DEV MODE] Using H2 in-memory database`
3. Visit http://localhost:8080/
   - Verify home page shows "User Management System"
   - Verify "Active Profile" shows "dev"
   - Verify H2 Console link is visible
4. Visit http://localhost:8080/h2-console
   - JDBC URL: jdbc:h2:mem:userdb
   - User: sa, Password: (empty)
   - Connect and verify app_user, app_role, user_role tables exist
   - Verify test user tiziano exists in app_user table with ADMIN role
5. Visit http://localhost:8080/actuator/health
   - Verify returns {"status":"UP"}
6. Stop the application (Ctrl+C)

**Docker Compose Verification:**
1. Ensure .env file has real passwords set
2. Run: `source ./bin/env.sh load && ./bin/env.sh substitute-all`
3. Run: `docker compose up --build`
4. Wait for all services to show "healthy" status
5. Verify container names: usermgmt-db, usermgmt-pgadmin, usermgmt-app
6. Check app logs for: `[PROD MODE] Using PostgreSQL`
7. Visit http://localhost:8080/
   - Verify home page shows "Active Profile: prod"
8. Visit http://localhost:5050/ (PgAdmin)
   - Login with credentials from .env
   - Verify "User Management DB" server is pre-listed (from servers.json)
   - Connect and verify database tables exist
9. Run: `docker compose down` to stop
10. Test db-only: `docker compose up db pgadmin` (starts only DB + PgAdmin)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 1 Success Criteria (from ROADMAP.md):
1. [x] Application starts in dev mode with H2 in-memory database using Spring `dev` profile
2. [x] Application starts in prod mode with PostgreSQL using Spring `prod` profile
3. [x] `docker compose up` launches App + PostgreSQL + PgAdmin and all containers reach healthy state
4. [x] Flyway executes initial schema migration on startup in both dev and prod profiles
5. [x] `.env` values are injected into application config via `.template` file processing, and setup script bootstraps a working environment from scratch
</verification>

<success_criteria>
- Human verified all 5 Phase 1 success criteria
- No blocking issues identified
- Phase 1 ready to mark complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-bootstrap--infrastructure/01-09-SUMMARY.md`
</output>
