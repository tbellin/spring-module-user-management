---
phase: 11-github-repository-setup--ci--readme
plan: 03
type: execute
wave: 3
depends_on:
  - "11-02"
files_modified:
  - README.md
autonomous: true
requirements:
  - GH-05

must_haves:
  truths:
    - "README.md first heading is followed immediately by the CI badge linking to the GitHub Actions workflow"
    - "README.md Quick Start clone URL points to the live github.com/tbellin/<repo> address (not a placeholder)"
    - "Badge URL uses the exact workflow filename ci.yml (not main.yml or build.yml)"
    - "README change is committed to git so the badge renders on GitHub after push"
  artifacts:
    - path: "README.md"
      provides: "Updated README with CI badge and real clone URL"
      contains: "github.com/tbellin/"
  key_links:
    - from: "README.md CI badge"
      to: "github.com/tbellin/<repo>/actions/workflows/ci.yml"
      via: "Markdown image link pointing to ci.yml workflow runs page"
      pattern: "actions/workflows/ci\\.yml/badge\\.svg"
    - from: "README.md Quick Start"
      to: "github.com/tbellin/<repo>.git"
      via: "git clone URL in bash code block"
      pattern: "git clone https://github\\.com/tbellin/"
---

<objective>
Update README.md with the CI badge and the live GitHub repository clone URL, then commit the change.

Purpose: The phase goal requires that README.md reflects the live repository URL and CI build status. Without this update, users cloning from GitHub see a placeholder clone URL and no CI badge — the repo looks unfinished. This plan closes requirement GH-05.

Output: README.md with a CI badge after the title and a real git clone URL in Quick Start. One git commit carrying the change.
</objective>

<execution_context>
@/Users/tiziano/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tiziano/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Read confirmed repo name from Plan 02 summary before editing README
@.planning/phases/11-github-repository-setup--ci--readme/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read confirmed repo name and update README.md with CI badge and clone URL</name>
  <files>README.md</files>
  <action>
    Step 1 — Read the confirmed repository name from 11-02-SUMMARY.md.
    The summary records the name chosen at the Plan 02 checkpoint (e.g., "user-management").
    Extract it; call it REPO_NAME. Do NOT hard-code a name — always read from the summary.

    Step 2 — Construct the two strings to insert (substitute REPO_NAME):
      Badge line:
        [![CI](https://github.com/tbellin/REPO_NAME/actions/workflows/ci.yml/badge.svg)](https://github.com/tbellin/REPO_NAME/actions/workflows/ci.yml)

      Clone URL (for Quick Start section):
        https://github.com/tbellin/REPO_NAME.git

    Step 3 — Edit README.md with these exact changes:

    Change A — Insert badge after the first heading (line 1: "# Spring Boot User Management Server").
    Insert a blank line followed by the badge line, followed by a blank line, so the result is:

      # Spring Boot User Management Server

      [![CI](https://github.com/tbellin/REPO_NAME/actions/workflows/ci.yml/badge.svg)](https://github.com/tbellin/REPO_NAME/actions/workflows/ci.yml)

      A Spring Boot 4 user management server...

    Do NOT place the badge at the bottom of the file or inside a section. It must be immediately after the title, before the description paragraph.

    Change B — In the "Quick Start" section, replace the placeholder clone URL:
      Old: git clone &lt;repo-url&gt;
      New: git clone https://github.com/tbellin/REPO_NAME.git

    Also update the cd placeholder if present:
      Old: cd &lt;project-dir&gt;
      New: cd REPO_NAME

    Do NOT change any other lines. Do NOT reformat or reorder sections. Do NOT add a "Repository" section or any other new content.

    Step 4 — Verify the badge URL uses exactly "ci.yml" as the workflow filename (not "main.yml", "build.yml", or any other name). The workflow created in Plan 01 is .github/workflows/ci.yml — the badge URL must match exactly.
  </action>
  <verify>
    Run: grep -n "github.com/tbellin" README.md
    Expected: at least two lines — the badge image/link and the clone URL — both containing the actual repo name (not a placeholder).

    Run: grep "ci.yml/badge.svg" README.md
    Expected: exactly one line containing the badge SVG URL with ci.yml.

    Run: grep "repo-url\|project-dir" README.md
    Expected: no output (placeholders fully replaced).

    Run: head -6 README.md
    Expected: title on line 1, blank line, badge on line 3, blank line, description paragraph.
  </verify>
  <done>
    README.md contains the CI badge immediately after the title heading, using the exact URL pattern https://github.com/tbellin/REPO_NAME/actions/workflows/ci.yml/badge.svg. The Quick Start clone URL is https://github.com/tbellin/REPO_NAME.git. No placeholder strings remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the README update</name>
  <files>README.md</files>
  <action>
    Stage and commit the README.md change with the exact commit message specified below.

    Step 1 — Stage:
    ```bash
    git add README.md
    ```

    Step 2 — Confirm only README.md is staged (not other unintended files):
    ```bash
    git diff --cached --name-only
    ```
    Expected output: README.md (only)

    Step 3 — Commit:
    ```bash
    git commit -m "docs(readme): add CI badge and GitHub repository URL"
    ```

    Use this commit message exactly — no variation. It is referenced in planning context as the canonical message for this change.

    Step 4 — Confirm commit was created:
    ```bash
    git log --oneline -1
    ```
    Expected: shows the new commit with the message above.
  </action>
  <verify>
    Run: git log --oneline -1
    Expected: commit hash followed by "docs(readme): add CI badge and GitHub repository URL"

    Run: git show --stat HEAD
    Expected: shows README.md with insertions, no other files changed.
  </verify>
  <done>
    git log shows the commit "docs(readme): add CI badge and GitHub repository URL" as HEAD. git show --stat HEAD confirms only README.md was modified in the commit.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. Badge present and correctly formed:
   ```bash
   grep "ci.yml/badge.svg" README.md
   ```
   Must return a line containing: `[![CI](https://github.com/tbellin/<REPO_NAME>/actions/workflows/ci.yml/badge.svg)](https://github.com/tbellin/<REPO_NAME>/actions/workflows/ci.yml)`

2. Clone URL updated:
   ```bash
   grep "git clone" README.md
   ```
   Must return: `git clone https://github.com/tbellin/<REPO_NAME>.git`

3. No placeholders remain:
   ```bash
   grep -E "repo-url|project-dir" README.md
   ```
   Must return empty.

4. Badge is positioned correctly (after title, before description):
   ```bash
   head -6 README.md
   ```
   Line 1: `# Spring Boot User Management Server`
   Line 2: (blank)
   Line 3: `[![CI](...badge.svg)](...)`
   Line 4: (blank)
   Line 5: `A Spring Boot 4 user management server...`

5. Commit exists with exact message:
   ```bash
   git log --oneline -1
   ```
   Must show: `docs(readme): add CI badge and GitHub repository URL`
</verification>

<success_criteria>
- README.md has the CI badge immediately after the title, using the exact ci.yml workflow URL
- README.md Quick Start section has git clone https://github.com/tbellin/REPO_NAME.git (no placeholder)
- No placeholder strings ("repo-url", "project-dir") remain in README.md
- Commit "docs(readme): add CI badge and GitHub repository URL" is the HEAD commit
- Requirement GH-05 is fully satisfied: README reflects the live repository URL and CI build status
</success_criteria>

<output>
After completion, create `.planning/phases/11-github-repository-setup--ci--readme/11-03-SUMMARY.md`
</output>
