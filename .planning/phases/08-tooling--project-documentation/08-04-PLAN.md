---
phase: 08-tooling--project-documentation
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "README.md provides a project overview and quick-start instructions"
    - "README.md links to all four doc/ files"
    - "README.md references bin/ scripts (run-dev.sh, run-prod.sh, test-api.sh)"
    - "A developer landing on the repo understands what the project is and how to get started"
  artifacts:
    - path: "README.md"
      provides: "Project overview, feature list, quick-start, links to docs and scripts"
      contains: "Quick Start"
  key_links:
    - from: "README.md"
      to: "doc/01-setup.md"
      via: "Markdown link to detailed setup guide"
      pattern: "doc/01-setup\\.md"
    - from: "README.md"
      to: "doc/02-architecture.md"
      via: "Markdown link to architecture docs"
      pattern: "doc/02-architecture\\.md"
    - from: "README.md"
      to: "doc/03-api-reference.md"
      via: "Markdown link to API reference"
      pattern: "doc/03-api-reference\\.md"
    - from: "README.md"
      to: "doc/04-deployment.md"
      via: "Markdown link to deployment guide"
      pattern: "doc/04-deployment\\.md"
---

<objective>
Create the project README.md that serves as the entry point for developers, linking to all documentation and scripts.

Purpose: The README is the first thing a developer sees when landing on the repository. It must convey what the project is, how to get started, and where to find detailed information.

Output: A README.md at the project root with overview, features, quick-start, script reference, and links to all doc/ files.
</objective>

<execution_context>
@/Users/tiziano/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tiziano/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Documentation files this README links to (created by 08-03)
@doc/01-setup.md
@doc/02-architecture.md
@doc/03-api-reference.md
@doc/04-deployment.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with project overview and documentation links</name>
  <files>README.md</files>
  <action>
Create `README.md` at the project root with the following sections:

1. **Title and tagline**:
   ```
   # Spring Boot User Management Server

   A Spring Boot 4 user management server with Spring Modulith architecture, JWT authentication, and dual-database support (H2 for development, PostgreSQL for production).
   ```

2. **Features**: Bulleted list of key capabilities:
   - Self-registration with email verification
   - JWT-based authentication (login/logout) with remember-me
   - Password management (change, forgot, reset via email)
   - User profile (view, edit)
   - Admin user management (CRUD, search, filter, enable/disable)
   - Role-based access control (ADMIN, USER)
   - REST API under /api/v1/ with Swagger UI documentation
   - Thymeleaf pages with Bootstrap 5
   - Spring Modulith enforced module boundaries
   - Dual environment: H2 (dev) / PostgreSQL + Docker Compose (prod)

3. **Tech Stack**: Compact table:
   | Component | Technology |
   | Framework | Spring Boot 4.0.1, Spring Framework 7 |
   | Language | Java 21 |
   | Architecture | Spring Modulith (Auth, User, Shared modules) |
   | Security | Spring Security, JWT (JJWT), BCrypt |
   | Database | H2 (dev), PostgreSQL (prod), Flyway migrations |
   | UI | Thymeleaf, Bootstrap 5 |
   | API Docs | SpringDoc OpenAPI 3.0.1, Swagger UI |
   | Build | Maven (wrapper included) |
   | Deployment | Docker Compose (App + PostgreSQL + PgAdmin) |

4. **Quick Start**:
   ```bash
   # Clone and setup
   git clone <repo-url>
   cd <project-dir>
   ./bin/setup.sh          # Generate .env from template
   # Edit .env with your SMTP credentials

   # Dev mode (H2 in-memory database)
   ./bin/run-dev.sh

   # Prod mode (Docker Compose stack)
   ./bin/run-prod.sh
   ```
   Then: Open http://localhost:8080 | Swagger UI: http://localhost:8080/swagger-ui.html

   See [Setup Guide](doc/01-setup.md) for detailed instructions.

5. **Scripts**: Table of bin/ scripts:
   | Script | Purpose |
   | `bin/run-dev.sh` | Start in dev mode (H2, local) |
   | `bin/run-prod.sh` | Start production stack (Docker Compose) |
   | `bin/test-api.sh` | Run cURL API tests |
   | `bin/setup.sh` | First-time project setup |
   | `bin/env.sh` | Environment and config management CLI |
   | `bin/check-port.sh` | Check/free port conflicts |

6. **Test User**: Pre-seeded admin account for dev mode: email `tizianobellin@yahoo.com`, password `password123` (ADMIN role, email verified).

7. **Documentation**: Links to all doc/ files:
   | Document | Description |
   | [Setup Guide](doc/01-setup.md) | Prerequisites, installation, first run |
   | [Architecture](doc/02-architecture.md) | Module structure, tech stack, design decisions |
   | [API Reference](doc/03-api-reference.md) | Endpoint catalog with cURL examples |
   | [Deployment](doc/04-deployment.md) | Docker Compose production deployment |

8. **API Testing**: Brief section showing how to run the test suite:
   ```bash
   # Start the app first, then:
   ./bin/test-api.sh
   ```

9. **License**: "This project is for educational purposes." (or omit if not relevant)

**Style guidelines:**
- Keep README scannable — use tables and code blocks, not prose paragraphs
- Link to doc/ files for details rather than duplicating content
- Do NOT reference doc/rate-limit-options.md (informal scratch note, not official docs)
- Do NOT use emojis
  </action>
  <verify>
Check README.md exists: `ls README.md`. Check it links to all doc files: `grep -c 'doc/' README.md` should be 4+. Check it references key scripts: `grep -c 'bin/' README.md` should be 6+. Check it has Quick Start section: `grep 'Quick Start' README.md`.
  </verify>
  <done>README.md exists at project root with: project overview, feature list, tech stack table, quick-start instructions, scripts table, test user info, documentation links to all 4 doc/ files, and API testing section.</done>
</task>

</tasks>

<verification>
1. README.md exists at project root
2. README.md links to all four doc/ files (01-setup, 02-architecture, 03-api-reference, 04-deployment)
3. README.md references all key bin/ scripts (run-dev, run-prod, test-api, setup, env)
4. README.md has Quick Start with dev and prod commands
5. README.md has feature list and tech stack
</verification>

<success_criteria>
- README.md serves as the single entry point for developers discovering the project
- All doc/ files are linked and described
- Quick-start instructions get a developer from clone to running in under 5 minutes
</success_criteria>

<output>
After completion, create `.planning/phases/08-tooling--project-documentation/08-04-SUMMARY.md`
</output>
