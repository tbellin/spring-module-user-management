---
phase: 04-email-verification
plan: 06
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - src/main/resources/templates/auth/resend-verification.html
  - src/main/java/com/example/usermanagement/auth/internal/AuthWebController.java
autonomous: true

must_haves:
  truths:
    - "Resend page allows users to request new verification email"
    - "GET /auth/resend-verification shows email input form"
    - "POST /auth/resend-verification handles submission with rate limiting"
  artifacts:
    - path: "src/main/resources/templates/auth/resend-verification.html"
      provides: "Form to request new verification email"
      contains: "resend"
    - path: "src/main/java/com/example/usermanagement/auth/internal/AuthWebController.java"
      provides: "Web endpoints for resend verification"
      contains: "resend-verification"
  key_links:
    - from: "AuthWebController"
      to: "ResendRateLimiter"
      via: "canResend check before sending"
      pattern: "rateLimiter\\.canResend"
    - from: "AuthWebController"
      to: "AuthService.sendVerificationEmail"
      via: "method call on form submit"
      pattern: "authService\\.sendVerificationEmail"
---

<objective>
Create the resend verification page and add web resend endpoints to AuthWebController.

Purpose: Provides the web-based resend verification flow. Users who missed or lost their verification email can request a new one through a dedicated form page.

Output: Resend verification Thymeleaf page with GET/POST endpoints in AuthWebController.
</objective>

<execution_context>
@/Users/tiziano/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tiziano/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-email-verification/04-CONTEXT.md
@.planning/phases/04-email-verification/04-RESEARCH.md

Prior plan summaries needed:
@.planning/phases/04-email-verification/04-04-SUMMARY.md

Source files to reference:
@src/main/resources/templates/layout/default.html
@src/main/resources/templates/auth/login.html
@src/main/java/com/example/usermanagement/auth/internal/AuthWebController.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create resend verification page</name>
  <files>src/main/resources/templates/auth/resend-verification.html</files>
  <action>
Create the resend form page per CONTEXT.md ("Dedicated resend page where user enters email"):

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="~{layout/default}">
<head>
    <title>Resend Verification Email</title>
</head>
<body>
<div layout:fragment="content">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-4">
            <div class="card shadow">
                <div class="card-body p-4">
                    <h4 class="card-title text-center mb-4">Resend Verification Email</h4>

                    <!-- Success Message (shown after submission) -->
                    <div th:if="${message}" class="alert alert-success" role="alert">
                        <span th:text="${message}"></span>
                    </div>

                    <!-- Error Message -->
                    <div th:if="${error}" class="alert alert-danger" role="alert">
                        <span th:text="${error}"></span>
                    </div>

                    <!-- Form -->
                    <form th:action="@{/auth/resend-verification}" method="post" th:unless="${message}">
                        <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}"/>

                        <div class="mb-3">
                            <label for="email" class="form-label">Email Address</label>
                            <input type="email" class="form-control" id="email" name="email"
                                   placeholder="Enter your email" required>
                            <div class="form-text">
                                Enter the email address you used to register.
                            </div>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary">Send Verification Email</button>
                        </div>
                    </form>

                    <!-- Back to login link -->
                    <div class="text-center mt-3">
                        <a th:href="@{/auth/login}" class="text-decoration-none">Back to Login</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>
```
  </action>
  <verify>Template file exists at correct path</verify>
  <done>resend-verification.html provides form for requesting new verification email</done>
</task>

<task type="auto">
  <name>Task 2: Add web resend endpoints to AuthWebController</name>
  <files>src/main/java/com/example/usermanagement/auth/internal/AuthWebController.java</files>
  <action>
Add GET and POST endpoints for the resend verification page. The AuthWebController already handles /auth/* web routes.

1. Add dependencies for rate limiter and auth service:
```java
private final ResendRateLimiter rateLimiter;

// Update constructor to include rateLimiter
public AuthWebController(AuthService authService, ResendRateLimiter rateLimiter) {
    this.authService = authService;
    this.rateLimiter = rateLimiter;
}
```

2. Add the endpoints:
```java
/**
 * Shows the resend verification form.
 */
@GetMapping("/resend-verification")
public String showResendVerificationForm() {
    return "auth/resend-verification";
}

/**
 * Handles resend verification form submission.
 * <p>
 * SEC-01 compliance: Returns the same message regardless of whether
 * the account exists, is already verified, or doesn't exist.
 */
@PostMapping("/resend-verification")
public String resendVerification(@RequestParam String email, Model model) {
    // Check rate limit
    if (!rateLimiter.canResend(email)) {
        long remaining = rateLimiter.getRemainingCooldownSeconds(email);
        model.addAttribute("error", "Please wait " + remaining + " seconds before requesting another email.");
        return "auth/resend-verification";
    }

    // Send email (if account exists and is unverified)
    // SEC-01: Same response regardless of outcome
    authService.sendVerificationEmail(email);
    rateLimiter.recordResend(email);

    model.addAttribute("message", "If an account exists with this email, a verification link has been sent.");
    return "auth/resend-verification";
}
```

3. Add required import:
```java
import com.example.usermanagement.auth.internal.verification.ResendRateLimiter;
```
  </action>
  <verify>`./mvnw compile` succeeds</verify>
  <done>GET /auth/resend-verification shows form, POST handles submission with rate limiting</done>
</task>

</tasks>

<verification>
1. Compile succeeds: `./mvnw compile`
2. Application starts: `./mvnw spring-boot:run -Dspring-boot.run.profiles=dev`
3. Resend page accessible: Open http://localhost:8080/auth/resend-verification in browser
</verification>

<success_criteria>
- resend-verification.html provides email input form
- AuthWebController has GET/POST endpoints for /auth/resend-verification
- Web resend flow enforces rate limiting
- All pages use consistent layout (layout/default.html)
</success_criteria>

<output>
After completion, create `.planning/phases/04-email-verification/04-06-SUMMARY.md`
</output>
